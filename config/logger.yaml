logging:
  level: INFO              # DEBUG / INFO / WARNING / ERROR
  log_dir: logs
  file_prefix: app
  console:
    enabled: true
  file:
    enabled: true
    rotate: daily           # daily / size
    max_size_mb: 20         # rotate=size 时生效
    backup_count: 10
